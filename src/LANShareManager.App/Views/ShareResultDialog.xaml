<Window x:Class="LANShareManager.App.Views.ShareResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Общий ресурс успешно создан" 
        Height="540" Width="540"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        FontFamily="Segoe UI, Inter, Arial"
        FontSize="13">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <Border Background="{StaticResource SuccessBgBrush}" CornerRadius="12" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="✓" Foreground="{StaticResource SuccessBrush}" FontWeight="Bold" FontSize="14" />
                </Border>
                <TextBlock Text="Общий доступ успешно настроен" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock Text="Папка готова к работе и доступна устройствам в вашей локальной сети." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Result Card -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Paths -->
                    <TextBlock Text="Локальный путь:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2" />
                    <TextBox x:Name="TxtLocalPath" IsReadOnly="True" Background="#F9F9F9" Padding="6" Margin="0,0,0,12" BorderBrush="{StaticResource CardBorderBrush}" />

                    <TextBlock Text="Сетевой путь (Network path):" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2" />
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtHostnamePath" IsReadOnly="True" FontWeight="SemiBold" Background="#F9F9F9" Padding="6" BorderBrush="{StaticResource CardBorderBrush}" />
                        <Button Grid.Column="1" Content="Копировать" Style="{StaticResource SecondaryButton}" Height="30" Margin="6,0,0,0" Click="BtnCopyHostname_Click" />
                    </Grid>

                    <TextBlock Text="IP путь (IP path):" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2" />
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtIpPath" IsReadOnly="True" Background="#F9F9F9" Padding="6" BorderBrush="{StaticResource CardBorderBrush}" />
                        <Button Grid.Column="1" Content="Копировать" Style="{StaticResource SecondaryButton}" Height="30" Margin="6,0,0,0" Click="BtnCopyIp_Click" />
                    </Grid>

                    <!-- How to connect from other PC -->
                    <Border Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="💡 Басқа компьютерден қалай қосылу керек? (Win + R)" FontWeight="Bold" Foreground="#15803D" FontSize="13" Margin="0,0,0,4" />
                            <TextBlock Text="1. Басқа компьютердің пернетақтасынан Win + R түймелерін басыңыз." Foreground="#166534" FontSize="12" Margin="0,0,0,2" />
                            <TextBlock Text="2. 'Выполнить' терезесіне мына жолды көшіріп қойыңыз (IP жолы)." Foreground="#166534" FontSize="12" Margin="0,0,0,2" />
                            <TextBlock Text="3. Enter басыңыз — ортақ папка лезде ашылады!" Foreground="#166534" FontSize="12" Margin="0,0,0,8" />
                            <Button x:Name="BtnOpenFullConnectGuide" Content="🌐 Толық нұсқаулықты көру (Подробная инструкция Win+R)..." Style="{StaticResource SecondaryButton}" Height="28" HorizontalAlignment="Left" Click="BtnOpenFullConnectGuide_Click" />
                        </StackPanel>
                    </Border>

                    <Separator Margin="0,0,0,14" />

                    <!-- Checklist Status -->
                    <TextBlock Text="Статус компонентов:" FontWeight="Bold" Margin="0,0,0,10" />

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Служба SMB:" />
                        <TextBlock x:Name="TxtSmbStatus" Grid.Column="1" Text="✓ Включено" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" />
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Разрешения общего доступа SMB:" />
                        <TextBlock x:Name="TxtSharePermStatus" Grid.Column="1" Text="✓ Настроены" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" />
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Разрешения безопасности NTFS:" />
                        <TextBlock x:Name="TxtNtfsStatus" Grid.Column="1" Text="✓ Настроены" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" />
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Брандмауэр Windows (TCP 445):" />
                        <TextBlock x:Name="TxtFirewallStatus" Grid.Column="1" Text="✓ Настроен" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" />
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Диагностика подключения:" />
                        <TextBlock x:Name="TxtConnectivityStatus" Grid.Column="1" Text="✓ Проверено" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" />
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Actions -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Открыть папку" Style="{StaticResource SecondaryButton}" Height="34" Margin="0,0,8,0" Click="BtnOpenFolder_Click" />
                <Button x:Name="BtnShowDiagnostics" Content="Отчет диагностики" Style="{StaticResource SecondaryButton}" Height="34" Click="BtnShowDiagnostics_Click" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Копировать сетевой путь" Style="{StaticResource PrimaryButton}" Height="34" Margin="0,0,8,0" Click="BtnCopyHostname_Click" />
                <Button Content="Закрыть" Style="{StaticResource SecondaryButton}" Width="90" Height="34" IsCancel="True" Click="BtnClose_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
